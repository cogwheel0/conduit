// Moved to the core so the desktop daemon serves channels through the
// same providers; the mobile app keeps importing it from here.
export 'package:conduit_core/features/channels/providers/channel_providers.dart';
