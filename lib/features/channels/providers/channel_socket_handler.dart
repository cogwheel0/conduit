// Moved to the core (M5) so the desktop daemon serves channels through the
// same providers; the mobile app keeps importing it from here.
export 'package:conduit_core/features/channels/providers/channel_socket_handler.dart';
