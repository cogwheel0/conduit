// Moved to the core so the desktop daemon serves notes through the
// same providers; the mobile app keeps importing it from here.
export 'package:conduit_core/features/notes/providers/notes_providers.dart';
