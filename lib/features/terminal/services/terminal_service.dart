// Moved to the core so the desktop daemon serves the terminal through
// the same code; the mobile app keeps importing it from here.
export 'package:conduit_core/features/terminal/services/terminal_service.dart';
